# RFC-00 — Pinned upstream contract

RFC begins only from explicitly pinned upstream states. The bootstrap lock is stored in `CROSS_REFERENCE_LOCK.json`.

## TIR contribution

TIR supplies the relational/informational primitives, phase geometry, Kähler/Berry structure and canonical information coefficient used by downstream candidate operators.

## Secret of a Half contribution

Secret of a Half supplies the binary-complement / critical-half structure and the spinorial \(2\pi/4\pi\) cross-reference used to test double-cover and phase-cancellation mechanisms.

## IDT contribution

IDT supplies dynamic time, temporal phase connection, transport, local elapsed-time/clock structure, NOW/bifurcation and the \(2\pi/4\pi\) time lift.

## Source-document contribution

`Lambda_0.pdf` supplies the prior dynamic-vacuum proposal in which the Einstein sector has the structural form

\[
G_{\mu\nu}+\Lambda_0(x)g_{\mu\nu}
=\frac{8\pi G}{c^4}T^{\rm total}_{\mu\nu}.
\]

`Neutrinotime14.pdf` supplies a later tensor-scalar-time formulation and an on-shell conservation relation for a field-dependent \(\Lambda_0\).

These equations are cross-referenced as **targets and prior constructions**. RFC must re-derive the admissible \(\Lambda_0\) dependence from the pinned upstream primitives rather than assume its final functional form.
