# TIR × IDT × RFC Local Spacetime Carrier Dimension Closure v0.7

Status: **CANDIDATE / EXACT LOCAL-CARRIER DIMENSION THEOREM / PHYSICAL SPACETIME SOLDERING CANDIDATE**

## 1. Inputs

The current cross-repository chain supplies two typed local carriers.

TIR spatial geometry supplies

\[
V_S=\operatorname{Herm}_0(2)
=\operatorname{span}_{\mathbb R}\{\sigma_1,\sigma_2,\sigma_3\},
\]

with

\[
\boxed{\dim_{\mathbb R}V_S=3.}
\]

IDT temporal-trace uniqueness supplies the rotational-scalar carrier

\[
V_T=\mathbb RI,
\]

with

\[
\boxed{\dim_{\mathbb R}V_T=1.}
\]

The IDT uniqueness theorem selects the temporal scalar functional, up to one positive clock calibration constant, as

\[
\boxed{T(X)=\alpha\operatorname{Tr}X,\qquad \alpha>0.}
\]

## 2. Exact carrier decomposition

The primitive two-level observable carrier is

\[
V=\operatorname{Herm}(2).
\]

For every \(X\in V\), define

\[
P_T(X)=\frac12\operatorname{Tr}(X)I,
\]

\[
P_S(X)=X-P_T(X).
\]

Then

\[
P_T(X)\in V_T,
\qquad
P_S(X)\in V_S,
\]

and

\[
\boxed{X=P_T(X)+P_S(X).}
\]

The intersection satisfies

\[
\boxed{V_T\cap V_S=\{0\}.}
\]

Therefore

\[
\boxed{
V=V_T\oplus V_S
=\mathbb RI\oplus\operatorname{Herm}_0(2).
}
\]

## 3. Dimension theorem

The direct sum gives the lower-rank certificate

\[
\dim V
\ge
\dim V_T+\dim V_S
=1+3=4.
\]

Independently,

\[
\dim_{\mathbb R}\operatorname{Herm}(2)=4.
\]

Hence the lower and upper bounds coincide:

\[
\boxed{
\dim_{\mathbb R}V=4.
}
\]

Thus the minimal complete local carrier generated by the admitted primitive two-level observable algebra contains exactly

\[
\boxed{3\ \text{spatial directions}+1\ \text{temporal scalar direction}.}
\]

Equivalently,

\[
\boxed{D_{\rm local\ carrier}=3+1=4.}
\]

## 4. Premetric transversality certificate

Write

\[
X=x^0I+\sum_{i=1}^3x^i\sigma_i
\]

with dual coordinates

\[
\theta^0(A)=\frac12\operatorname{Tr}A,
\qquad
\theta^i(A)=\frac12\operatorname{Tr}(A\sigma_i).
\]

For

\[
e_0=I,
\qquad
e_i=\sigma_i,
\]

the exact evaluation matrix is

\[
\boxed{[\theta^\mu(e_\nu)]=I_4.}
\]

Therefore

\[
\boxed{
\theta^0\wedge\theta^1\wedge\theta^2\wedge\theta^3
(e_0,e_1,e_2,e_3)=1.
}
\]

This gives an exact four-rank premetric coframe certificate.

The RFC v0.5 executable reference verifies the trace/traceless projectors, dual pairing, fail-closed input handling and the worldline-motion firewall with local result

```text
8 passed in 0.08s
```

## 5. Temporal uniqueness certificate

Let a real extensive temporal functional on the Hermitian carrier be

\[
T(X)=a_0x^0+\mathbf a\cdot\mathbf x.
\]

Spatial frame covariance acts by the full \(SO(3)\) action on \(\mathbf x\). Invariance of \(T\) under this action yields

\[
\boxed{\mathbf a=0.}
\]

and therefore

\[
\boxed{T(X)=\alpha\operatorname{Tr}X.}
\]

Positive temporal orientation fixes

\[
\boxed{\alpha>0.}
\]

and reference-clock calibration fixes the remaining scale in declared units.

The IDT v0.6 executable reference verifies the rotational constraints, additivity, positivity and fail-closed input handling with local result

```text
7 passed in 0.08s
```

## 6. Lorentzian determinant consequence

On the same carrier write

\[
X=x^0I+x^1\sigma_1+x^2\sigma_2+x^3\sigma_3.
\]

Then

\[
\boxed{
\det X
=(x^0)^2-(x^1)^2-(x^2)^2-(x^3)^2.
}
\]

The primitive four-dimensional carrier therefore carries a canonical nondegenerate quadratic form of Lorentzian inertia.

RFC RF-G0 remains the downstream signature/physicalization layer, now supplied with an independently established four-rank carrier rather than using the dimension as an upstream premise.

## 7. Minimality and completeness within the primitive carrier

The admitted spatial sector already requires three independent traceless Hermitian generators. The temporal uniqueness theorem adds one independent scalar Hermitian generator. Their direct sum has rank four.

The full primitive observable carrier has real dimension four, so the dimension closure is complete inside the admitted \(\mathbb C^2\) Hermitian algebra.

Any additional local base direction therefore requires an independently derived carrier beyond this primitive Hermitian algebra and its established spatial/temporal decomposition.

## 8. Dependency order

The corrected local chain is

\[
\boxed{
\mathbb C^2
\to
\operatorname{Herm}_0(2)
\to
r_S=3
}
\]

and

\[
\boxed{
\mathrm{IDT\ extensive\ temporal\ scalar}
\to
\operatorname{Tr}
\to
\mathbb RI
\to
r_T=1.
}
\]

Then

\[
\boxed{
r_S+r_T=3+1=4}
\]

followed by

\[
\boxed{
\operatorname{Herm}(2)
\xrightarrow{\det}
\text{Lorentzian quadratic form}
\to
\text{RFC field closure}.
}
\]

## 9. Status ledger

| Statement | Status |
|---|---|
| TIR spatial carrier rank = 3 | CURRENT TIR/RFC STRUCTURAL RESULT |
| IDT temporal invariant scalar class = trace up to positive scale | EXACT CANDIDATE THEOREM |
| `Herm(2) = RI ⊕ Herm_0(2)` | EXACT |
| premetric temporal/spatial transversality | EXACT |
| local carrier dimension = 4 | EXACT |
| determinant Lorentzian inertia | EXACT |
| reference validations | 8/8 RFC PASS + 7/7 IDT PASS |
| physical spacetime soldering / global field realization | DOWNSTREAM CANDIDATE |

The formalism may suggest that the exact four-dimensional local carrier is the physical spacetime tangent carrier, yet does not state that implication as an established result before the TIR spatial physicalization and RFC soldering/field-closure gates pass.
