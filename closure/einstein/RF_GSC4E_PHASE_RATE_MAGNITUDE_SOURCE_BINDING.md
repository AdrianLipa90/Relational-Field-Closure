# RF-GSC4E — Overlap-Local Phase-Magnitude Source-Binding Firewall

Status: `EXACT_SPATIAL_SCALE_QUOTIENT / EXECUTABLE_OVERLAP_LOCAL_MAGNITUDE_BINDING_CERTIFIER / PRODUCTION_MAGNITUDE_FIELD_OPEN`

Date: 2026-08-31

## 1. Purpose

RF-GSC4D v0.2 represents the rigid-route spatial scale as the pointwise field

\[
\boxed{s(x)=\frac{c}{\sqrt6\,|\omega_t(x)|}},
\qquad
\boxed{e(x)=s(x)I_3.}
\]

RF-GSC4E isolates the exact information content of the pointwise overlap-scale condition and removes signed phase-rate identity from the spatial geometry packet.

## 2. Magnitude quotient

For every physical sample `x`, define

\[
\boxed{\nu(x):=|\omega_t(x)|>0.}
\]

Then

\[
\boxed{s(x)=\frac{c}{\sqrt6\,\nu(x)}.}
\]

The map

\[
\nu\mapsto \frac{c}{\sqrt6\nu}
\]

is injective on the positive reals. Hence, for two patch representations of the same overlap point,

\[
\boxed{s_p(x)=s_q(x)\iff\nu_p(x)=\nu_q(x).}
\]

The GSC4D spatial scale therefore factors through the pointwise quotient

\[
\boxed{\omega_t(x)\sim-\omega_t(x).}
\]

A sign reversal preserves the magnitude, spatial scale, physical coframe scale and the rigid overlap relation.

## 3. Field semantics

The source-binding condition is local to each represented physical point. Distinct samples may carry distinct magnitudes:

\[
\nu(x)\neq\nu(y)
\]

while each sample separately satisfies its patch-gluing equality.

Thus a connected patch cover supports spatially varying

\[
\boxed{a(x)=\frac{c}{\sqrt6\nu(x)}}
\]

and preserves the RF-02I connection sector generated by derivatives of `ln a`.

## 4. Minimal production witness

For each overlap-local physical sample `x_alpha`, the source packet contains:

1. source and target patch identifiers;
2. one physical `sample_id`;
3. one common `clock_id` / temporal calibration;
4. one common `phase_magnitude_field_id`;
5. finite nonzero signed phase-rate representatives in both charts;
6. the pointwise equality
   \[
   \boxed{|\omega_{t,p}(x_\alpha)|=|\omega_{t,q}(x_\alpha)|}
   \]
   within the declared numerical tolerance.

A signed phase-rate identity is a stronger, separately typed input.

## 5. Executable certifier

Implementation:

`src/rfc/phase_rate_magnitude_binding.py`

Reference tests:

`tests/reference/test_gsc4e_phase_rate_magnitude_binding.py`

The certifier records each overlap-local sample independently and audits:

- finite nonzero representatives;
- physical sample identity;
- clock provenance;
- magnitude-field provenance;
- pointwise magnitude agreement;
- pointwise spatial-scale agreement;
- signed-rate agreement as an informationally stronger diagnostic.

A connected cover may pass with one magnitude on one overlap sample and another magnitude on another sample. A sign flip may also pass the spatial binding while signed-rate equality remains false.

## 6. Typed boundary

The GSC4E result supplies the spatial phase-scale quotient used by the rigid GSC4D route. The following carriers remain separately owned downstream or upstream:

- IDT relational lapse and its calibration;
- IDT event-clock exactness;
- signed phase-rate orientation/dynamics;
- production magnitude-field provenance and physical sample identities.

## 7. Claim ledger

| Statement | Status |
|---|---|
| `s(x)` factors through `nu(x)=|omega_t(x)|` | `EXACT` |
| pointwise `s_p=s_q iff nu_p=nu_q` | `EXACT` |
| sign reversal preserves the GSC4D spatial scale | `EXACT` |
| connected cover supports varying `nu(x)` | `EXACT REPRESENTATION PROPERTY` |
| executable overlap-local magnitude certifier | `VALIDATION TARGET` |
| production magnitude-field/sample provenance | `OPEN SOURCE INPUT` |
| signed phase-rate equality | `SEPARATE STRONGER INPUT` |
| IDT lapse binding | `SEPARATE TEMPORAL INPUT` |

## 8. Live 36D boundary

The magnitude reduction was independently audited through the active

`GREMLIN -> Terminal36D -> PhaseNav36D -> GREMLIN`

surface with `CANDIDATE_ONLY` authority. Deterministic hosted validation remains the executable evidence surface for this candidate theorem.
