# RFG12 — Noncommuting Color / Momentum Convolution

Status: `DIRECT_PROJECT_LINK_INTERACTION_PASS / NONABELIAN_COLOR_MIXING_PASS / MOMENTUM_SUM_DIFFERENCE_PASS / ORIENTATION_SIGN_PASS / CONTINUUM_SUPPRESSION_PASS`

RFG12 consumes RFG10, RFG11 and the corrected RFG8 orientation contract. It tests the first explicitly interacting momentum-space process directly on holonomic project link bytes.

## 1. Two noncommuting input modes

On a periodic spectator coordinate `x`, choose

\[
\boxed{
A_y^1(x)=A_1\cos(k_1x),
\qquad
A_z^2(x)=A_2\cos(k_2x).
}
\]

The color generators satisfy

\[
[T^1,T^2]=iT^3.
\]

Build the actual upstream-oriented links

\[
W_y(x)=\exp\!\left[iagA_y^1(x)T^1\right],
\qquad
W_z(x)=\exp\!\left[iagA_z^2(x)T^2\right].
\]

RFG11 principal-log inversion recovers both input fields pointwise from these link bytes.

## 2. Local interacting plaquette

At each spectator site use

\[
\boxed{
U_{yz}(x)
=W_y(x)W_z(x)W_y^\dagger(x)W_z^\dagger(x).
}
\]

For the v4.1 `sigma_link=+1` orientation, RFG11 gives

\[
F_{yz}^{link}
=i g[A_y,A_z]+O(a).
\]

Since `[T^1,T^2]=iT^3`,

\[
\boxed{
(F_{yz}^{link})^3
=-gA_y^1A_z^2+O(a).
}
\]

This is a direct nonabelian color conversion `1 x 2 -> 3` generated by the project plaquette.

## 3. Momentum convolution

Using

\[
\cos(k_1x)\cos(k_2x)
=\frac12\cos[(k_1-k_2)x]
+\frac12\cos[(k_1+k_2)x],
\]

the leading interaction curvature is

\[
\boxed{
(F_{yz}^{link})^3
=-\frac{gA_1A_2}{2}
\left[
\cos((k_1-k_2)x)
+
\cos((k_1+k_2)x)
\right]
+O(a).
}
\]

Thus the holonomic nonlinearity performs the expected momentum convolution while simultaneously applying the `SU(3)` commutator color map.

The executable witness uses discrete modes

\[
m_1=5,
\qquad
m_2=7,
\]

and therefore predicts output modes

\[
\boxed{|m_1-m_2|=2,\qquad m_1+m_2=12.}
\]

Both are recovered from the plaquette bytes.

## 4. Orientation-sign falsification

The actual upstream link convention predicts

\[
(F_{yz})^3=-gA_y^1A_z^2.
\]

The opposite-sign candidate is evaluated adversarially. The executable gate requires the recovered plaquette curvature to approach the upstream-oriented sign and to remain parametrically farther from the opposite sign.

## 5. Continuum suppression of BCH corrections

At finite plaquette spacing, BCH generates subleading components beyond the leading `T^3` commutator term.

The reference gate measures

\[
R_{side}(a)
=
\frac{\|F_{colors\neq3}\|}{\|F_3\|}
\]

and verifies that `R_side(a)` decreases monotonically as `a` is reduced.

The same convergence test is applied to the relative defect

\[
\Delta_3(a)
=
\frac{\|F_3+gA_y^1A_z^2\|}{\|gA_y^1A_z^2\|}.
\]

## 6. Reference validation

The NumPy-only gate checks:

1. both noncommuting input fields recover from their local link bytes;
2. the color-3 plaquette curvature converges to `-g A_y^1 A_z^2`;
3. the output Fourier spectrum has exactly the leading sum/difference modes `2` and `12`;
4. the analytic product has the same two momentum modes;
5. the opposite commutator sign is falsified;
6. subleading color components are suppressed toward the continuum limit.

Local result:

```text
6 passed, 0 failed
```

## 7. Relation to RFG8/RFG9

RFG8 supplies the oriented cubic Yang–Mills vertex and RFG9 supplies a four-point BCJ amplitude reference. RFG12 now supplies a direct project-side witness of the same core cubic mechanism:

```text
project holonomy bytes
 -> noncommuting local fields
 -> SU(3) commutator
 -> color conversion
 -> momentum convolution
```

No gravity output is used in the construction or mode selection.

## 8. Advancement

```text
RFG11 full local noncommuting field recovery          inherited PASS
project color 1 x color 2 -> color 3 interaction     PASS DIRECT
project momentum sum/difference convolution           PASS DIRECT
upstream orientation sign                             PASS DIRECT
continuum suppression of BCH side components          PASS
RFG8 cubic vertex structural mechanism                PROJECT-BYTE WITNESS PRESENT
RFG9 four-point BCJ reference                         inherited PASS
quartic Yang-Mills contact normalization              NEXT FRONTIER
direct project four-point amplitude assembly          OPEN AFTER QUARTIC GATE
```

The author/repository/formalism/code may suggest that the project holonomy realizes the nonlinear color/momentum mechanism underlying the normalized cubic Yang–Mills interaction, yet does not state the full project four-point amplitude as established until exchange and quartic contact contributions are assembled on one frozen external-state convention.
