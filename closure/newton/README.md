# Newton closure

Newton remains a mandatory limiting gate reached from the relational lapse/source construction.

## Current derived structure

RF-N0 supplies the relational potential

\[
\boxed{\Phi_R=c^2\ln N_R}
\]

from the IDT clock ratio `N_R=d tau_x/d tau_ref`. Static zero-shift geodesic kinematics gives

\[
\frac{d^2X^i}{dt^2}
=-N_R^2h^{ij}\partial_j\Phi_R+\cdots,
\]

which reduces near the reference clock and locally Euclidean spatial frame to

\[
\boxed{\frac{d^2X^i}{dt^2}=-\partial^i\Phi_R+\cdots.}
\]

RF-N1A combines the IDT Shannon--Onsager graph response with the six-direction hexahedral frame and yields

\[
\boxed{
\Delta_h\ln N_R=\mathcal S_R,
\qquad
\Delta_h\Phi_R=c^2\mathcal S_R.
}
\]

RF-N1B through RF-N1B2N factorize the conserved source carrier, phase-action energy and calibrated normal phase rate.

RF-N1B2O now supplies a local phase-kinetic matter-source realization:

\[
\boxed{
j_\vartheta=2A^2\omega_Q,
\qquad
\rho_\vartheta=\frac{\omega_Q}{2c^2}j_\vartheta
=\frac{A^2\omega_Q^2}{c^2},
}
\]

with

\[
\omega_Q=D_{\hat\tau}\chi=\frac{D_t\chi}{N_R}.
\]

Thus the carrier-to-mass-density map is derived on this admitted phase-kinetic source sector.

## RF-N1C coupling bridge

The Newton coupling coordinate is

\[
\boxed{
G_N^{(\vartheta)}
=\frac{c^4\mathcal S_R}{2\pi\omega_Qj_\vartheta}.
}
\]

The independently gated double-copy route gives, in natural units on the local carrier-scale candidate,

\[
\boxed{
G_{DC}
=\frac{18\Gamma_{DC}^2}
{\pi\beta_W^2\omega_Q^2}.
}
\]

Equating the two routes produces the G-free source relation

\[
\boxed{
\beta_W^2\mathcal S_R\omega_Q
=36\Gamma_{DC}^2j_\vartheta.
}
\]

RF-N1C1 then uses `j_theta=2 A^2 omega_Q` to reduce this to

\[
\boxed{
\mathcal S_R^{DC}
=\frac{72\Gamma_{DC}^2}{\beta_W^2}A^2.
}
\]

The local phase-rate dependence cancels exactly from the source curvature, while

\[
\rho_\vartheta\propto\omega_Q^2,
\qquad
G_{DC}\propto\omega_Q^{-2}
\]

so that `4 pi G_DC rho_theta` remains equal to the same `S_R^DC`.

## Universality firewall

RF-N1C2 isolates the source-independent gravity scale

\[
\boxed{
\bar M_G
:=\frac{M_\star}{\Gamma_{DC}g_{YM}^2},
\qquad
G_{DC}=\frac{1}{8\pi\bar M_G^2}.
}
\]

With the Wilson coordinate,

\[
\boxed{
\bar M_G
=\frac{\beta_WM_\star}{6\Gamma_{DC}}.
}
\]

On the local carrier-scale candidate `M_star=omega_Q/2`,

\[
\boxed{
\bar M_G^{local}
=\frac{\beta_W\omega_Q}{12\Gamma_{DC}}.
}
\]

Universal Newton coupling across independently admitted weak-field systems is therefore equivalent to source-independence of `Mbar_G`.

The current status is:

`FORCE_KINEMATICS_PASS / LOCAL_SOURCE_OPERATOR_PASS / PHASE_KINETIC_SOURCE_BINDING_PASS_CONDITIONAL / NEWTON_DOUBLE_COPY_SOURCE_HOLONOMY_PASS_ALGEBRAIC / UNIVERSAL_G_CROSS_SYSTEM_GATE_OPEN`.
