# Newton closure

Newton remains a mandatory limiting gate rather than an independently inserted theory.

## Current derived structure

RF-N0 supplies the relational potential

\[
\boxed{\Phi_R=c^2\ln N_R}
\]

from the IDT clock ratio `N_R=d tau_x/d tau_ref`. Static zero-shift geodesic kinematics gives

\[
\frac{d^2X^i}{dt^2}
=-N_R^2h^{ij}\partial_j\Phi_R+\cdots,
\]

which reduces near the reference clock and locally Euclidean spatial frame to

\[
\boxed{\frac{d^2X^i}{dt^2}=-\partial^i\Phi_R+\cdots.}
\]

RF-N1A combines the IDT Shannon--Onsager graph response with the six-direction hexahedral frame. The normalized equal-neighbour operator satisfies

\[
\boxed{
-\frac1{a_H^2}L_H
\longrightarrow\Delta_h
}
\]

and the signed-permutation symmetry classification independently forces the leading local constant-coefficient scalar second-order operator to be proportional to `Delta`; the physical stencil fixes the principal normalization.

Thus the current Newton-facing field balance is typed as

\[
\boxed{
\Delta_h\ln N_R=\mathcal S_R,
\qquad
\Delta_h\Phi_R=c^2\mathcal S_R,
\qquad
[\mathcal S_R]=L^{-2}.
}
\]

## Open source gate

`S_R` is not yet derived. `Xi_I` is retained only as a candidate source-basis scalar because `[Xi_I]=L^-2`:

\[
\mathcal S_R=\beta_I\Xi_I+\cdots.
\]

No coefficient is fitted here.

The Poisson target

\[
\boxed{
\Delta\Phi_R\stackrel{?}{=}4\pi G\rho_m
}
\]

is tested only after an independent derivation of the source map and normalization. Therefore the present status is:

`FORCE_KINEMATICS_PASS_CANDIDATE / LOCAL_SOURCE_OPERATOR_PASS / SOURCE_IDENTIFICATION_OPEN / G_OPEN`.
