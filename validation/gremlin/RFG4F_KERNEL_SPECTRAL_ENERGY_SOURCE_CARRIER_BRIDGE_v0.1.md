# RFG4F — Kernel Spectral-Energy ↔ Source-Carrier Bridge

Status: `CHYBA / CANDIDATE_ONLY / HISTORICAL_KERNEL_IDENTITIES_RECOVERED / NORMALIZED_SPECTRAL_CONSISTENCY_EXACT / SOURCE_CARRIER_SOLDERING_OPEN / DIMENSIONAL_FIREWALL_ACTIVE`

## 1. Purpose

RFG4F compares the historical CIEL Unified Reality Kernel definition of `alpha_c` with the current RFC finite Noether-carrier energy coordinate.

The historical kernel paper supplies an energy/occupation identity and an independent spectral-spacing identity for the same coordinate. The current RFC source reduction supplies

\[
\boxed{
\epsilon_N^{EB}=\frac12D_\tau\chi.
}
\]

RFG4F defines the exact algebraic compatibility conditions among these coordinates while keeping the historical normalized-kernel energy and the current RFC carrier energy separately typed until an independent unit/normalization soldering is supplied.

## 2. Historical kernel energy coordinate

The October-2025 *Unified Reality Kernel* gives the constant-verification relation

\[
\boxed{
\alpha_c^{(E)}
:=
\frac{\langle E_{\rm consciousness}\rangle}
{\langle n\rangle\hbar_{\rm eff}}.
}
\]

The same source lists normalized effective constants including

\[
\hbar_{\rm eff}=0.892345,
\qquad
c_{\rm eff}=0.956712,
\]

and later derives the spectral peak spacing

\[
\boxed{
\omega_n
=n\alpha_c\frac{c_{\rm eff}}{\hbar_{\rm eff}}.
}
\]

Therefore the spectral estimator is

\[
\boxed{
\alpha_c^{(\omega)}
:=
\frac{\omega_n\hbar_{\rm eff}}
{n c_{\rm eff}}.
}
\]

For one internally consistent normalized-kernel state,

\[
\boxed{
\alpha_c^{(E)}=\alpha_c^{(\omega)}=\alpha_c.
}
\]

This is an exact algebraic consistency theorem once the historical normalized inputs are supplied.

## 3. Historical extraction witness

The paper reports that Fourier analysis of 1,024 simulation runs yields

\[
\boxed{
\alpha_c=0.474812\pm0.000007.
}
\]

RFG4B retains the historical extraction replay debt. RFG4C separately defines a clean-room spectral estimator. RFG4F uses only the recovered formulas and their mutual consistency.

## 4. Current RFC finite source-carrier energy

RF-N1B2L gives, after the admitted collective scalar-field → rotor reduction and Euler/Berry closure,

\[
\boxed{
\epsilon_N^{EB}
=\frac{H_\Phi^{EB}}{Q_\vartheta^{EB}}
=\frac12D_\tau\chi.
}
\]

This is the current RFC energy-per-carrier coordinate on the admitted common phase sector.

## 5. Soldering coordinate

Introduce an independently determined normalization/units soldering `s_E` from the RFC carrier-energy coordinate into the historical normalized-kernel energy coordinate:

\[
\boxed{
\langle E_{\rm consciousness}\rangle
=s_E\,\epsilon_N^{EB}.
}
\]

Then the source-carrier estimate of the kernel coordinate is

\[
\boxed{
\alpha_c^{(N)}
=
\frac{s_E D_\tau\chi}
{2\langle n\rangle\hbar_{\rm eff}}.
}
\]

The exact compatibility condition is

\[
\boxed{
\alpha_c^{(N)}
=
\alpha_c^{(E)}
=
\alpha_c^{(\omega)}.
}
\]

The soldering `s_E` must come from an independently specified units/normalization map. It cannot be selected by minimizing the alpha defect.

## 6. Executable defects

Define

\[
\boxed{
\Delta_{E\omega}
=
\frac{|\alpha_c^{(E)}-\alpha_c^{(\omega)}|}
{|\alpha_c^{(E)}|}
}
\]

and

\[
\boxed{
\Delta_{N\alpha}
=
\frac{|\alpha_c^{(N)}-\alpha_c|}{|\alpha_c|}.
}
\]

For the historical archive coordinate, one may also audit

\[
\boxed{
\Delta_{N,archive}
=
\frac{|\alpha_c^{(N)}-0.474812|}{0.474812}.
}
\]

A diagnostic inverse coordinate is

\[
\boxed{
 s_E^{req}
=
\frac{2\alpha_c\langle n\rangle\hbar_{\rm eff}}
{D_\tau\chi}.
}
\]

` s_E^{req}` reports the soldering required for equality. Its value has diagnostic authority until independently generated by a units/normalization theorem.

## 7. Dimensional firewall

The October-2025 kernel uses normalized effective coordinates. RFC's current `epsilon_N` belongs to the contemporary phase/source normalization.

Physical promotion requires an explicit dimensional map specifying:

- dimensions and normalization of the historical `E_consciousness`;
- dimensions/normalization of `hbar_eff` and `c_eff` in the historical simulation;
- relation of historical simulation time to current `tau` / calibrated proper-time coordinates;
- occupation-number definition `n`;
- the independently selected soldering `s_E`.

Until these are bound, RFG4F authorizes normalized-kernel cross-consistency and a candidate source-carrier bridge. The Newton/double-copy chain retains the existing RFG2/RFG3 dimensional gates.

## 8. Falsification contract

The bridge must fail when any independently supplied coordinate is detuned while the others are held fixed. In particular:

1. perturbing a spectral peak must make `Delta_Eomega > 0`;
2. perturbing the carrier rate at fixed `s_E` must make `Delta_Nalpha > 0`;
3. changing occupation number without the corresponding energy/spectral change must fail consistency;
4. setting `s_E` from the target alpha inside the same validation path is classified as circular and denied promotion.

## 9. GREMLIN verdict

The historical kernel supplies a genuine energy/occupation and spectral interpretation of `alpha_c`. The current RFC supplies a finite phase-energy-per-carrier coordinate. Their common algebra is now explicit and testable.

The physical soldering

\[
E_{\rm consciousness}
\longleftrightarrow
\epsilon_N^{EB}
\]

remains the single new bridge coordinate introduced at this gate and retains `CHYBA / CANDIDATE_ONLY` authority pending an independent units/normalization derivation.
