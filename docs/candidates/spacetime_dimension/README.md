# Spacetime-dimension candidate chain

Status: `CANDIDATE_ONLY / NO_CANON_PROMOTION`

This directory preserves the staged pre-RF-G0 dimension derivation.

## Candidate lineage

1. `PRE_RF_G0_SPACETIME_RANK_GATE_V0_4.md`
   - established the premetric four-volume factorization;
   - identified `PREMETRIC_TEMPORAL_SPATIAL_TRANSVERSALITY` as the remaining algebraic gate.

2. `PREMETRIC_TEMPORAL_SPATIAL_TRANSVERSALITY_V0_5.md`
   - supersedes the **OPEN transversality status** of v0.4;
   - proves the trace/traceless direct sum
     `Herm(2) = R I ⊕ Herm_0(2)`;
   - proves the canonical dual evaluation matrix is `I4`;
   - closes carrier/coframe transversality before any metric or four-dimensional-base premise;
   - preserves the firewall that carrier transversality does not imply zero spatial worldline velocity.

## Current frontier

The algebraic premetric transversality gate is now `PASS` at candidate level.

The remaining physical binding is:

`IDT_ELAPSED_SCALE_AS_HERMITIAN_TRACE_COORDINATE`

After that binding, the local dimension consequence is

`D_local_base = 4`,

and RF-G0 may be applied downstream for Lorentzian signature rather than assuming a four-dimensional base upstream.

No file in this directory is canon authority by merge status alone.
