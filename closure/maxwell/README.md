# Maxwell closure

Target sequence:

1. derive a local one-form connection \(A\),
2. derive curvature \(F=dA\),
3. recover gauge redundancy \(A\mapsto A+d\chi\),
4. obtain \(dF=0\) as an identity,
5. derive the sourced dynamical equation from an admitted action/current,
6. recover charge-current conservation,
7. identify the empirical coupling/unit normalization only after the structural derivation.

**Current status:** OPEN.
